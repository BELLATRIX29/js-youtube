"use strict"; //treat as older version javascript to an new version.

//alert (3+3) // not give a output because we dont use browser, we used nodejs
// number => 2 to the 53 if we have to is use bigger number than we have to use
// bigint
// string => "" we prefer to used " "
// boolean => true/false (yes/no)
// null => standlone value
// undefine=> we did not put a vaule iin the box
// symbol=> unique

// object

console.log(typeof undefined) // data type undefined
console.log(typeof null) // object